<div class="d-flex justify-content-between align-items-center mb-4">
  <h1 class="h3 text-white">Seyirci Yorumları</h1>
  <%= link_to "Yeni Yorum Ekle", new_admin_testimonial_path, class: "btn btn-primary" %>
</div>

<div class="card bg-dark border-secondary shadow-lg">
  <div class="card-body p-0">
    <div class="table-responsive">
      <table class="table table-dark table-hover align-middle mb-0">
        <thead>
          <tr>
            <th class="ps-4 text-white-50">İsim</th>
            <th class="text-white-50">Ünvan</th>
            <th class="text-white-50">Yorum</th>
            <th class="text-center pe-4 text-white-50">İşlemler</th>
          </tr>
        </thead>
        <tbody>
          <% @testimonials.each do |testimonial| %>
            <tr>
              <td class="ps-4 fw-medium text-white"><%= testimonial.name %></td>
              <td class="text-white-50"><%= testimonial.title %></td>
              <td class="text-white-50 text-truncate" style="max-width: 300px;"><%= testimonial.comment %></td>
              <td class="text-center pe-4">
                <div class="btn-group btn-group-sm">
                  <%= link_to edit_admin_testimonial_path(testimonial.id), class: "btn btn-outline-light", title: "Düzenle" do %>
                    <i class="bi bi-pencil"></i>
                  <% end %>
                  <%= button_to admin_testimonial_path(testimonial.id), method: :delete, class: "btn btn-outline-danger", form: { class: "d-inline" }, form_class: "d-inline", title: "Sil", data: { turbo_confirm: "Emin misiniz?" } do %>
                    <i class="bi bi-trash"></i>
                  <% end %>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
